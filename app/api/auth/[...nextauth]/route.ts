// Handler NextAuth.js — obsługuje wszystkie endpointy /api/auth/*
// Instalacja: npm install next-auth
//
// import { handlers } from "@/lib/auth";
// export const { GET, POST } = handlers;

// Tymczasowy placeholder — aktywuj po instalacji next-auth
export async function GET() {
  return new Response("NextAuth nie jest jeszcze skonfigurowany.", { status: 501 });
}

export async function POST() {
  return new Response("NextAuth nie jest jeszcze skonfigurowany.", { status: 501 });
}
